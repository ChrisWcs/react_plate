import React from 'react';
import ReactDOM from 'react-dom';

import Title from './Title';

const App = () => (
    <div>
        <Title/>
    </div>
);

ReactDOM.render(
    <App/>,
    document.body
);
